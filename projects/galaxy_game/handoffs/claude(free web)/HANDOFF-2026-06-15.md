# Session Handoff — 2026-06-15
**Branch:** main
**Strategist:** Claude (Free Tier)
**Prepared:** End of 2026-06-15 evening session

---

## Baseline

- **ai_manager suite:** 746 examples, 3 failures (flaky only), 4 pending ✅
- **Last full suite:** Not run today — run fresh at session start
- **Last galaxyGame commit:** Task 1 + Task 2 changes (decision tree + training refresh)
- **Last agent-tasks commit:** File #9 triage archived, new phase5+ task created

---

## Completed Today

| Task | Agent | Result |
|---|---|---|
| AI Manager decision tree dead steps fix | Haiku 0.33x | ✅ resource_acquisition_logic_v1.json updated — NOTE: not loaded by any service yet |
| Luna AI Manager training refresh | Qwen3.5-27B | ✅ lunar_pattern populated, duplicate key removed, January rates preserved |
| April backlog file #9 triage | Qwen3.5-27B | ✅ STOP condition triggered — backup files have unique methods, new phase5+ task created |
| Ryzen 7 GPU swap | Manual | ✅ RX 9060 XT installed, Ollama confirmed running |
| Ollama model tags (nothink) | Manual | ⚠️ Tags generated but caused issues — resolved in separate session |
| ROUTING_LOGIC.md dual-system update | Claude + separate session | ✅ Updated and committed — reflects new hardware topology |

---

## Critical Finding From Today

**`resource_acquisition_logic_v1.json` is NOT loaded by any service.**
The decision tree JSON is an architectural specification document only. Real decision logic
is hard-coded in `ResourceAcquisitionService`. This means:
- Dead step fixes in the JSON have zero runtime impact currently
- Wiring `ResourceAcquisitionService` to read from config is required before fixes take effect
- This becomes the next implementation task after StateAnalyzer extension

**ResourceAcquisitionService research task is still pending** — 35B hit response-too-long
errors and could not complete the read. Needs to be dispatched fresh next session with
tighter output constraints.

---

## Ryzen 7 Setup State (as of end of session)

- **Model installed:** `qwen3.5:35b-a3b` ✅
- **Model pulling:** `qwen3.5:27b` (may still be in progress — verify)
- **Ollama tags:** Issues discovered with generated tags — check separate session notes
  for current tag state before dispatching anything to Ryzen
- **GPU split:** 34%/66% CPU/GPU on 35B — expected given 23GB model vs 16GB VRAM
- **Response length issue:** 35B generates responses too long for Copilot limit —
  needs `num_predict` cap, exact value TBD from separate session findings
- **M4 tags:** Not yet configured — handle in separate session on M4

---

## Dispatch Order for Next Session

1. **Verify Ryzen Ollama tag state** — check what was resolved in the other session
   before dispatching anything. Confirm nothink is working and response length is managed.

2. **Run ai_manager suite** to confirm baseline still clean:
   ```bash
   docker exec -it web bash -c 'unset DATABASE_URL && RAILS_ENV=test bundle exec rspec spec/services/ai_manager/ 2>&1 | tail -10'
   ```
   Expected: 746 examples, ≤3 failures (flaky only), 4 pending

3. **Dispatch ResourceAcquisitionService research** to Ryzen 35B with tight output
   constraint. Simple handoff — find the file, read it, report only:
   - File path and line count
   - Does it read resource_acquisition_logic_v1.json? YES/NO
   - How decisions are made (one sentence)
   - What calls it
   - What it returns

4. **Dispatch Task 3 — StateAnalyzer extension** after research results confirm
   insertion point. Adds celestial body context + net GCC flow to analyze_state.

5. **Base craft backup method merge review** — human decision needed on
   `phase5+/2026-06-15-MEDIUM-REFACTOR-BASE-CRAFT-BACKUP-METHOD-MERGE.md`
   before that task can be dispatched. Four methods found in backup files:
   - `crew_count()` — crew tracking
   - `mass()` — dry mass + inventory calculation
   - `physical_properties()` — blueprint dimension lookup
   - `can_process_volatiles?` — depot operations predicate

---

## Phase 5+ Task Queue (current state)

| File | Priority | Notes |
|---|---|---|
| `2026-06-15-HIGH-BUG-FIX-AI-MANAGER-DECISION-TREE-DEAD-STEPS.md` | HIGH | ✅ COMPLETE — JSON updated but service wiring still needed |
| `2026-06-15-HIGH-FEATURE-LUNA-AI-MANAGER-TRAINING-REFRESH.md` | HIGH | ✅ COMPLETE |
| `2026-06-15-MEDIUM-REFACTOR-BASE-CRAFT-BACKUP-METHOD-MERGE.md` | MEDIUM | 🔴 BLOCKED — human decision on method merge needed |
| `2026-06-12-MEDIUM-RESEARCH-AI-MANAGER-TRAINING-PIPELINE-AUDIT.md` | MEDIUM | ✅ COMPLETE |
| Atmospheric transfer service refactor | HIGH | Extracted from file #8 triage — blocks Luna simulation |
| StateAnalyzer extension (Task 3) | HIGH | Next implementation task — not yet created as task file |
| ResourceAcquisitionService config wiring | HIGH | Discovered today — not yet created as task file |

---

## Architecture Decisions Made Today

**Luna economic model confirmed:**
- Luna = extraction outpost, always import-dependent, never self-sufficient
- LDC owns Luna, mandatory infrastructure — must exist for GCC system
- Survival guaranteed by LDC + Earth backers
- Expansion governed by GCC flow (subsidy → optimization → expansion modes)
- Correct metric: export ROI, not self-sufficiency

**Worldhouse architecture confirmed:**
- LavaTube + Worldhouse + WorldhouseSegment models all exist and are world-agnostic
- Lava tube sealing works identically on Luna, Mars, asteroid cavities
- population_capacity calculated from enclosed_volume_km3
- AI Manager has no awareness of any of this — needs training + StateAnalyzer extension

**tasks_v2 confirmed as world-agnostic option library:**
- No world-locked assumptions in task files
- AI Manager should match conditions to options, not apply named world patterns
- Luna pattern = conditions under which option clusters succeed, not Luna-specific rules

**ResourceAcquisitionService is hard-coded:**
- resource_acquisition_logic_v1.json not loaded at runtime
- Service wiring task needed before dead step fixes have any effect

---

## Hardware Topology (current)

- **Ryzen 7 Windows** — Qwen3.5-35B primary heavy executor, 27B pulling
- **M4 Mac** — Qwen3.5-9B/27B, daytime work machine
- **Intel MacBook** — git/host operations only
- **Pi 4** — home server, reload still pending

---

## Flaky Specs — Never Touch

| Spec | Line |
|---|---|
| `spec/services/construction/orbital_shipyard_service_spec.rb` | 129 |
| `spec/services/generators/game_data_generator_spec.rb` | 13 |
| `spec/services/lookup/material_lookup_service_spec.rb` | 251 |

---

## Session Startup Checklist

1. `CLAUDE_FREE_GUIDE.md` — current copy
2. This handoff
3. Check Ryzen Ollama tag state from other session before dispatching
4. Run ai_manager suite — confirm 746/≤3/4 baseline
5. Review base craft method merge decision before dispatching phase5+ triage

Good night.
