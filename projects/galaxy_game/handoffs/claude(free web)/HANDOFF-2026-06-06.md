# Session Handoff — 2026-06-06
**Branch:** main
**Strategist:** Claude (Free Tier)
**Prepared:** End of 2026-06-06 session

---

## Baseline
- **ai_manager suite:** 731 examples, 0 failures, 4 pending
- **Last full suite:** 3948 examples, 3 failures (all confirmed flaky)
- **ACTION FIRST THING:** Run full suite and update status.md baseline before dispatching any tasks
  ```
  docker exec -it web bash -c 'cd /home/galaxy_game && unset DATABASE_URL && RAILS_ENV=test bundle exec rspec 2>&1 | tail -10'
  ```

---

## Completed This Session

| Task | Agent | Result |
|---|---|---|
| Luna Phase 2 — ManifestGenerator → AI Manager | Qwen3.5-27B (Continue) | ✅ 731 examples, 0 failures — commit 21b10ef0 |

---

## Infrastructure Changes This Session

**Copilot tool use restored** — qwen3.5:27b tool execution was broken since Ollama update (0.17.5 → 0.30.6). Fixed by re-pulling model weights: `ollama pull qwen3.5:27b` on M4. Root cause: Ollama 0.30.x changed tool call format for qwen3.5 family; model weights were updated March 2, 2026 — re-pull gets the fixed weights.

**Copilot agent file created** — `Ollama.agent.md` custom agent with tool permissions. Required for tool use in new Copilot windows. Keep tools list minimal: `['vscode', 'execute', 'read', 'edit', 'search']`

**settings.json updated** on Intel Mac:
```json
"chat.permissions.default": "bypassApprovals",
"chat.tools.global.autoApprove": true
```

**Continue confirmed as fallback** — works with 27B but has friction: manual approval per terminal command, no Apply button in current config, manual file edits required. Do not use `sed` for Ruby file edits — caused file corruption this session.

**Single model workflow confirmed** — 27B only per session. Model switching causes context accumulation and token limit errors. Confirmed by 27B agent's own analysis.

**Ollama on M4** — now running via official Mac app (not brew). Context length must be set manually in menu bar app after any reinstall. Current working value: 262144.

---

## Active Queue — Pick Up Next Session

| File | Agent | Notes |
|---|---|---|
| `2026-06-03-HIGH-BUG-FIX-IMPORT-REQUEST-GENERATOR-REMOVE-PLURAL-API.md` | Hold | `generate_import_requests` (plural) confirmed live in ServiceCoordinator. Human decision needed. |
| Luna Phase 3 — `Logistics::ShortageDetector` + `Logistics::ImportRequestGenerator` | Qwen3.5-27B | Unblocked. Task file not yet drafted. Drop ShortageDetector and ImportRequestGenerator files to strategist at session start. |

---

## Session Startup Checklist

Hand strategist these files at session start:
1. `CLAUDE_FREE_GUIDE.md` — updated copy
2. `status.md` — with full suite results filled in
3. `app/services/logistics/shortage_detector.rb` — needed to draft Phase 3 task file
4. `app/services/logistics/import_request_generator.rb` — needed to draft Phase 3 task file
5. Any synthesis reports awaiting review

**Before dispatching any task** — verify Copilot 27B tool use is working:
- Open Ollama agent window, select qwen3.5:27b
- Send full handoff prompt as first message (not a simple command)
- Confirm `Ran...` UI elements appear
- If broken: `ollama pull qwen3.5:27b` on M4, reload VS Code, retry

---

## Key Architecture Confirmed This Session

- `Logistics::ManifestGenerator.create_manifest(source, destination, items)` — confirmed public API
- `Settlements::CostAnalyzer.current_import_price(resource_name, settlement)` — confirmed exists, used by ManifestGenerator
- Cape Canaveral Spaceport = canonical Earth source settlement (seeded, owned by AstroLift)
- `execute_cost_reduction` — Phase 2 complete, real manifest creation with graceful fallback
- `default_quantity_for` returns stub 10 — Phase 3 wires real quantities from ShortageDetector
- Future architecture: LEO depot (LDC LOX supply) → L1 station → Luna. Multiple Earth launch sites planned in JSON config.

## Phase 3 Pre-work (for strategist at next session start)

Before Phase 3 task file can be written, strategist needs:
- `shortage_detector.rb` — confirm public API and what shortage data it returns
- `import_request_generator.rb` — confirm public API and inputs
- Confirm whether `execute_cost_reduction` should call ShortageDetector directly or consume its output from StateAnalyzer
- Confirm whether `default_quantity_for(resource)` stub in Phase 2 should be replaced by ShortageDetector quantities in Phase 3

Good night.
